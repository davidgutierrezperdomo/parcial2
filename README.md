CREATE TABLE users (
    username VARCHAR(50) PRIMARY KEY,
    password VARCHAR(100) NOT NULL,
    enabled BOOLEAN NOT NULL
);

CREATE TABLE authorities (
    username VARCHAR(50) NOT NULL,
    authority VARCHAR(50) NOT NULL,
    CONSTRAINT fk_authorities_users
        FOREIGN KEY (username)
        REFERENCES users(username)
        ON DELETE CASCADE
);

CREATE UNIQUE INDEX ix_auth_username
ON authorities (username, authority);

CREATE TABLE medicos (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL
);

CREATE TABLE consultas (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nombre_paciente VARCHAR(40),
    motivo_consulta VARCHAR(100),
    numero_consultorio INTEGER,
    hora_inicio TIME,
    hora_fin TIME,
    medico_id BIGINT,
    CONSTRAINT fk_consultas_medicos
        FOREIGN KEY (medico_id)
        REFERENCES medicos(id)
        ON UPDATE CASCADE
        ON DELETE SET NULL
);

INSERT INTO users (username, password, enabled) VALUES
('ADMIN-999', '{noop}secreto123', true),
('PAC-001', '{noop}secreto123', true),
('PAC-002', '{noop}secreto123', true),
('MED-101', '{noop}secreto123', true),
('MED-102', '{noop}secreto123', true),
('MED-103', '{noop}secreto123', true);

INSERT INTO authorities (username, authority) VALUES
('ADMIN-999', 'ROLE_ADMINISTRADOR'),
('PAC-001', 'ROLE_PACIENTE'),
('PAC-002', 'ROLE_PACIENTE'),
('MED-101', 'ROLE_MEDICO'),
('MED-102', 'ROLE_MEDICO'),
('MED-103', 'ROLE_MEDICO');

INSERT INTO medicos (id, nombre) VALUES
(1, 'Medico Uno'),
(2, 'Medico Dos'),
(3, 'Medico Tres');

INSERT INTO consultas (
    nombre_paciente,
    motivo_consulta,
    numero_consultorio,
    hora_inicio,
    hora_fin,
    medico_id
) VALUES
('Paciente Uno', 'Consulta general', 101, '08:00', '08:30', 1),
('Paciente Dos', 'Control medico', 102, '09:00', '09:30', 2);
